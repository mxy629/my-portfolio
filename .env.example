# 环境变量模板 —— 这个文件本身不含真实信息，可以安全提交。
#
# 使用方法：复制一份改名为 .env.local，填入真实值。
# .env.local 已被 .gitignore 忽略，不会被提交到 GitHub。
#
# 线上部署时，把同样的键配到 GitHub 仓库：
#   Settings → Secrets and variables → Actions → New repository secret

# 联系邮箱，会显示在网站的「联系」区块，并作为 mailto 链接。
VITE_CONTACT_EMAIL=your-email@example.com
